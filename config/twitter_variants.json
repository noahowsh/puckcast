{
  "morning_slate": [
    {
      "id": "A",
      "format": "morning_slate",
      "template": "Puckcast daily predictions for {date_label}\n{slate_lines}\n\n{team_tags}"
    }
  ],
  "team_highlight": [
    {
      "id": "A",
      "format": "team_highlight",
      "template": "Spotlight: {team_name}\nNext up: {opponent_label} ({start_time})\nModel lean: {favorite_team} {favorite_prob}%\nEdge: {edge_label}\n\nSee today’s predictions: {url}\n\n{team_tags}"
    }
  ],
  "results_recap": [
    {
      "id": "A",
      "format": "results_recap",
      "template": "{date_label} recap: {correct}/{total} correct ({accuracy}%)\nHits: {hits}\nMisses: {misses}\n\nFull recap + predictions: {url}"
    }
  ],
  "tomorrow_tease": [
    {
      "id": "A",
      "format": "tomorrow_tease",
      "template": "Tomorrow’s predictions drop at 8am ET ⏰ ({date_label})\n\nAnalytics for every game: {url}\n\n#NHL #HockeyTwitter"
    }
  ]
}
